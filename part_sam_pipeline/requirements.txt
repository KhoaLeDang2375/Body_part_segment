# ============================================================
# Requirements for part_sam_pipeline (runs in sam3env conda env)
# ============================================================

# --- SAM3 is installed separately via: pip install -e ../sam3 ---

# HTTP client for PartCATSeg server
httpx>=0.27.0

# UI
gradio>=4.20.0

# Core
numpy>=1.26,<2
Pillow>=10.0
scipy

# Server (for PartCATSeg inference_server.py — installed in partcatseg env)
# fastapi>=0.111
# uvicorn[standard]
